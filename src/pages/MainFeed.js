import React from "react";
import styled from "styled-components";

const MainFeed = () => {
  return <Wrapper>MainFeed</Wrapper>;
};

export default MainFeed;

const Wrapper = styled.div`
  height: 100vh;
`;
